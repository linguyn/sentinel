# 2026-09-12

## What I did

- add docs folder content: architecture, roadmap, devlog - track project progress 
- created Unity client using Universal 3D
- added Unity project to `unity-client/`
- updated `.gitignore`
- committed Unity project skeleton

## What I learned

- `Assets`, `Packages`, and `ProjectSettings` should be committed
- `Library` should not be committed because Unity regenerates it
- `.meta` files must be tracked because Unity uses GUIDs to reference assets

## Problems

VS Code showed:

> Git repository has too many active changes

Cause:
Unity generated thousands of files inside `Library`.

Solution:
Ignore Unity-generated directories in `.gitignore`.

## Next

- Create Unity API DTOs
- Add `Observation`
- Add `ActionResponse`